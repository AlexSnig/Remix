# Project Memory

Last updated: 2026-07-22

## Current state

- Repository: `https://github.com/AlexSnig/Remix`, branch `main`.
- Product: premium fully local Android museum motion-sensor APK for a dedicated kiosk phone.
- Current release: `1.3.1` (`versionCode` 6), built and installed on the A07 on 2026-07-22. Package `ua.alexsnig.exhibitmotion`. `v1.2.0` is tagged but its APK is defective — see the release-only R8 crash below.
- Release signing certificate SHA-256: `bfd47221742dfdb12763a42f7cafdfdcd74469bd712e9616cb3dfa2501100f7e` (RSA 4096, alias `exhibit-motion`, valid to 2053). Keystore lives outside the repo; Gradle credentials are in the gitignored `android/keystore.properties`.
- Runtime: signed Capacitor APK; React is the local operator WebView and Kotlin owns production behavior. No server or network is required.
- Audio: operator-imported local audio in app-private storage; only a verified AUX or named Bluetooth route is accepted, never the phone speaker.
- Route approval is an operator judgement: the sound test plays the file and waits for "Чую звук" (enabled after three seconds, enforced natively and in the UI), rather than requiring a long narration to reach its end. Only a person can confirm a speaker is audible.
- A 3.5 mm output has no identity on Android, so an AUX approval means "some wired output" and cannot be pinned to one speaker. That is required for unattended arming after a power cut. While the operator panel is open the approval is withdrawn when the output disappears, so a swapped cable is re-tested during commissioning. Bluetooth is matched by device name and stays strict.
- Bluetooth exhibits re-enable the radio at auto-start and wait up to 30 s for the approved speaker. Only a Device Owner may enable the radio from Android 13, and there is no public API to force an A2DP connection, so this depends on Android reconnecting a bonded device.
- Storage: Android DataStore owns settings/readiness and Room owns the bounded event log.
- Camera: CameraX foreground service at 36 × 48 analysis and at most 10 FPS; no simulated or browser fallback exists in the APK.
- Kiosk: Device Owner, persistent Home activity, Lock Task, operator PIN, visible-activity boot resume, and explicit ordinary-install behavior.
- Browser/PWA: retained only for UI and regression testing; Cloudflare is not a production dependency or release target.

## Validation snapshot

- `npm run lint`: passing.
- `npm run test:coverage`: 13 tests passing; 100% lines, 96.91% statements, 84.61% branches, 88.37% functions across selected critical utilities.
- `npm run build`: passing; main JS about 90 KB gzip; PWA precache about 646 KB.
- `npm run test:e2e`: 6/6 mobile/desktop arming, camera-denial, and offline service-worker scenarios passing.
- Browser plugin is unavailable, so regular Playwright is the accepted fallback.
- The signed release APK was installed on the real Galaxy A07 and driven through camera permission, audio import, AUX route test and arming. That is what caught the R8 defect below; keep doing it.
- Android native gate (`testDebugUnitTest`, `lintDebug`, `assembleDebug`, `assembleRelease`) passes on this workstation with JDK 21 and a local Android SDK/emulator (`BUILD SUCCESSFUL`, 11/11 unit tests, 0 lint errors). The release APK signature was independently verified with `apksigner` against the existing release key. This environment previously lacked Java/SDK/KVM; that gap has since been closed.

## Release-build hazard

Minified release builds are not covered by any automated check in this repository. On 2026-07-22 R8 stripped the Capacitor annotation types, which made `getPermissionState("camera")` throw a `NullPointerException` and killed the app on the first wizard step. Debug builds were unaffected because they are not minified, so lint, unit tests, Playwright, and a debug install on a second phone all reported success while the signed client APK was unusable.

Keep rules for `com.getcapacitor.annotation.**` and the plugin reflection surface now live in `android/app/proguard-rules.pro`. Do not remove them, and do not assume a green automated run says anything about the minified artifact. Before shipping any release APK, install that exact APK on a real phone and walk the operator wizard at least as far as granting camera permission. A quick structural check is to count annotation descriptors in the packaged DEX; a healthy build references `Lcom/getcapacitor/annotation/CapacitorPlugin;` at least once.

## Product decisions

- Native APK and Device Owner kiosk are the canonical production target.
- Runtime must be fully local. Google Drive, private servers, cloud logging, and visitor identification remain out of scope.
- Audio is copied into app-private Android storage before arming.
- Direct controlled installation is the release channel; Play Store and enterprise MDM distribution remain out of scope unless explicitly added later.

## Client handoff

- The client delivery package is assembled outside this repository, so the signing key can never be committed. It holds the release APK, staff PDF, Device Owner guide, a verification report, a build guide, and the signing key in an isolated subfolder with its own checksums so it can be delivered separately over a secure channel. The maintainer keeps its location privately; it is deliberately not recorded here.
- Superseded 1.1.0 artifacts were removed from the repository root and archived offline. That build predates the tagged history and exists nowhere else.
- The handoff keystore was verified end-to-end: re-signing an APK using only the files in the package reproduces the shipped certificate fingerprint exactly.

## Hardware in play

- **Galaxy A07 (`SM-A075F`, Android 15 / API 35)** — the museum target. Carries release `1.3.0`. This is the phone that matters for acceptance.
- **Galaxy S9 (`SM-G960F`, Android 10 / API 29)** — development phone, sits at the app's `minSdk`. Its debug build was removed on 2026-07-21 after backing up the app's private data.

## Open items as of 2026-07-22

Verified on the A07 this session: camera permission, sound test through AUX (`type:headphone`, `name:h2w`), route approval, arming, and **detection with the screen off** — `PARTIAL_WAKE_LOCK` holds, the camera stays open, and the service stays foreground. The screen may be switched off in the exhibition and should be, for heat and OLED burn-in.

**An app update does not re-arm the exhibit.** `adb install -r` kills the app; the relaunch restores Home and Lock Task, but `MotionDetectorService` only auto-starts from the boot path, so the sensor sits disarmed until a reboot or a manual arm. Always reboot after updating a commissioned phone.

**A reboot with no approved audio route also leaves it disarmed — by design.** Observed 2026-07-22 after the 1.3.1 update: `dumpsys audio` showed `Connected devices:` containing only `speaker`, the AUX having been unplugged during handling, and the service correctly refused to start rather than fall back to the handset speaker. Check the route before reading a quiet boot as a regression.

**Cold-boot resume re-verified on 1.3.1 (2026-07-22), AUX reconnected.** Service foreground from `intent={act=…AUTO_START}`, camera client held, `motion-detector` wake lock held, `mLockTaskModeState=LOCKED`, `MainActivity` on top, `dumpsys audio` showing the approved `type:0x8 (headphone) name:h2w`. No camera contention on this boot — only a CONNECT/DISCONNECT/CONNECT pair, without the EVICT/DENIED seen earlier, so that earlier race is intermittent rather than systematic. Watch it across the 8-hour run rather than treating it as settled. Note that arming from the panel produces `act=…action.START`; only the boot path produces `AUTO_START`, which is the distinction that proves an autostart claim.

**Cold-boot resume was first verified on 1.3.0 the same day**, after the operator finished the wizard and enabled autostart. `adb reboot`, then with no human touch: `topResumedActivity=ua.alexsnig.exhibitmotion/.MainActivity`, `mLockTaskModeState=LOCKED`, and `MotionDetectorService` running foreground from `intent={act=ua.alexsnig.exhibitmotion.action.AUTO_START}` — the boot path, not a manual start. `PARTIAL_WAKE_LOCK 'ua.alexsnig.exhibitmotion:motion-detector'` held from boot. One caveat worth watching in the 8-hour run: `dumpsys media.camera` shows a boot-time contention — our client was EVICTed and DISCONNECTed, a reconnect was DENIED with `Too many cameras already open, cannot open camera "1"`, and only the next attempt CONNECTed. It self-recovered within about a second and the camera is held now, but the Samsung camera app (`com.sec.android.app.camera/DepressStickerService`, holding its own wake lock) is the likely conflicting client. Watch `cameraRestarts` after boots. Note also that ADB did not return by itself after the reboot: the host `adb` server needed `kill-server`/`start-server` once the phone re-enumerated on USB.

Also confirmed on 2026-07-22: the A07 has **no secure lock screen** (`locksettings get-disabled: true`, `lockscreen.password_type: null`), so `KeyguardManager.isDeviceSecure` is false. That is the wanted state — the `secure_lock_requires_first_unlock` readiness blocker will not fire, and `applyDeviceOwnerPolicies` will call `setKeyguardDisabled(true)`, giving genuine no-touch startup after a power cut. Do not add a screen lock to this phone.

Blocked or undecided:

1. **Device Owner provisioned and kiosk policies applied (2026-07-22) — this item is done.** Verified after the operator pressed «Налаштувати Home і Lock Task»: `LockTaskPolicy {mPackages= ua.alexsnig.exhibitmotion; mFlags= 0 }`, HOME back to `.MainActivity`, and CAMERA / POST_NOTIFICATIONS / BLUETOOTH_CONNECT all `granted=true` with `POLICY_FIXED` — the grants sit at the end of `applyDeviceOwnerPolicies`, so reaching them proves the whole method ran, including `setKeyguardDisabled(true)`. **Verification trap:** on Android 15 this device does **not** print `mLockTaskPackages`; grepping for that name returns empty even when the policy is set. Grep for `LockTaskPolicy` or `-i ocktask` instead. An earlier conclusion in this session rested on that empty grep and was right only by luck — the UI indicator was the sound evidence. Historical detail below. The account blocker was real and is now gone: with a working ADB shell the A07 (`R8YY929R75R`) reported `Accounts: 0` and a single user (`UserInfo{0:Власник}`), so `adb shell dpm set-device-owner ua.alexsnig.exhibitmotion/.kiosk.ExhibitDeviceAdminReceiver` succeeded. `dumpsys device_policy` now shows `Device Owner Type: 0` with `admin=ExhibitDeviceAdminReceiver`, HOME resolves to `ua.alexsnig.exhibitmotion/.MainActivity`, and the system refuses `am force-stop` on the package ("Ignoring request to force stop protected package"). `device_provisioned=1`/`user_setup_complete=1` did not block the ADB path, because that check only rejects extra users or accounts and there were none. Samsung Knox Guard (`com.samsung.android.kgclient/.agent.KGDeviceAdminReceiver`) remains an enabled device admin with `wipe-data` alongside us; it did not interfere. **Still open:** `mLockTaskPackages` is empty. `ExhibitDeviceAdminReceiver.onProfileProvisioningComplete` fires only in the managed-provisioning flow, never for `dpm set-device-owner` over ADB, so `applyDeviceOwnerPolicies` has not run. That is deliberate — it is the operator's `configureKiosk` action, so Lock Task cannot start while audio import and calibration are unfinished. Until the operator runs it, `setLockTaskPackages` and `addPersistentPreferredActivity` stay unset and Lock Task and boot resume remain untested. Note that provisioning activates the HOME intent filter, so the exhibit becomes the launcher and leaving it needs the operator PIN; undoing it means a factory reset. Updates over the same signing key (`adb install -r`) preserve Device Owner. Factory Reset Protection: the Google account must be removed *before* any factory reset, or FRP will demand its password afterwards.
2. **Factory reset protection is still unread.** `factoryResetProtection` is produced only by the app's diagnostics (`MotionDetectorPlugin.factoryResetProtectionSupport`), which needs the operator panel — ADB cannot call it. ADB does confirm the hardware side exists: `dumpsys persistent_data_block` reports a real 1 MiB `/dev/block/by-name/frp` with `mIsFileBacked=false`. That proves storage exists and nothing more; the diagnostic probes whether Samsung implements `getFactoryResetProtectionPolicy`, which is a different question. Read it now that Device Owner exists. **The only route to that value is «Експорт JSON»** — the diagnostics panel renders just events, camera frames, restarts, errors and battery (`NativeDetectorPanel.tsx:442`), never `factoryResetProtection`. On 2026-07-22 the operator reported the JSON export failing. `exportDiagnostics` writes the file to the app's private `cacheDir/diagnostics/` *before* opening the share chooser, so the data exists but is unreachable from a release build (no `run-as`, shell cannot read app-private storage); the failure is in the `ACTION_SEND` chooser, plausibly because this phone now launches the exhibit as Home and has no app registered for `application/json`. Surfacing the field directly in the diagnostics list would remove the dependency on sharing entirely. `unsupported_by_manufacturer` means a stolen exhibit can be wiped and resold, which the museum must be told plainly. Physical mounting is the primary control either way.
3. **OS updates are blocked by ADB workaround, not by policy (2026-07-22).** A One UI 8 upgrade prompt appeared on the A07; an unattended upgrade would reboot the exhibit and could break it. Since `setSystemUpdatePolicy` is not implemented in the app, the stop-gap was `pm disable-user --user 0` on `com.wssyncmldm`, `com.samsung.android.app.updatecenter` and `com.sec.android.soagent`, plus `settings put global ota_disable_automatic_update 1`. All three confirmed in `pm list packages -d`. This is reversible with `pm enable` and **does not survive a factory reset**; it is also not a managed-device guarantee. The durable fix is a Device Owner `SystemUpdatePolicy` (postpone allows at most 30 days; freeze periods allow up to 90 days a year) — see the hardening item below.
4. **Device Owner hardening is offered but not built** — `DISALLOW_FACTORY_RESET`, `DISALLOW_SAFE_BOOT`, `DISALLOW_DEBUGGING_FEATURES`, `DISALLOW_ADD_USER`, `setStatusBarDisabled`. Note that disabling debugging also cuts ADB access, so it belongs last, after the night test.
5. **Decided 2026-07-22 — the narration is the 14-second greeting; the four-minute problem does not exist.** The operator confirmed that `+Сходи.MP3` is the exhibition greeting and the only file that ships. Measured: **14.1 s**, 192 kbps, md5 `36cd8534…`, byte-identical to `ExhibitMotion-озвучка-1.mp3`. The other two (`-2.mp3` 3:56, `-3.mp3` 4:04) were text drafts; the operator authorised deleting them and they were removed from `/sdcard/Download` (copies retained offline in this session's scratchpad only — not a durable backup). With a 14 s clip the trigger-to-re-arm window is short, so trim/restart/cap options are moot and 100+ triggers in an 8-hour run is comfortably reachable. The master copy now lives outside the repository at `/home/alex/exhibit-audio/+Сходи.MP3` and is also on the phone at `/sdcard/Download/`; it still has to be imported through the wizard into app-private storage to count.
6. **Decided 2026-07-22 — the front camera stays.** The operator confirmed the phone is mounted screen outward, so `CAMERA_FACING_FRONT` is correct and no code change is needed. Calibration binds to that lens. The screen may still be switched off in the exhibition: screen-off detection was verified on the A07 and the front camera is unaffected by it.
7. **The client handoff package still holds the defective 1.2.0 APK** and must be rebuilt from 1.3.0 or later before delivery. The `v1.2.0` tag points at that defective build.
8. **The 8-hour acceptance run has not been done.**
9. **The A07 drops ADB every time its screen locks — root cause identified 2026-07-22.** Symptom: `lsusb` keeps showing the phone (`04e8:6860`) with a live `iInterface 10 ADB Interface`, while `adb devices` goes empty and `adb logcat` hangs. Direct log evidence: a continuous capture died two seconds after the lock screen appeared, with `I/UsbPortManager: USB HAL HIDL version: 13` followed by `I/android.hardware.usb@1.3-service: Userspace turn off USB data signaling`, surrounded by `LockscreenNotificationIconsOnlyController` and `KeyguardSecAffordanceView` chatter. This is Samsung's lock-screen USB blocking (Auto Blocker family), **not** our app: `grep` confirms the codebase never calls `setUsbDataSignalingEnabled`, and `DISALLOW_DEBUGGING_FEATURES` was deliberately never applied. Note the phone has no *secure* credential (`isDeviceSecure` false) but still shows a swipe keyguard, which is enough to trigger the blocking. Two false starts to avoid repeating: restarting the adb server appears to fix it, but only because the screen happened to be on at that moment; and USB debugging was never actually disabled. Workarounds while commissioning: keep the screen awake (Developer options → Stay awake) or turn off Auto Blocker under Security and privacy. **The durable fix is completing kiosk configuration** — `applyDeviceOwnerPolicies` calls `setKeyguardDisabled(admin, true)` whenever `isDeviceSecure` is false (`KioskPolicyController.kt:88`), removing the keyguard and with it the trigger. **Resolved once the kiosk was configured, and verified on 2026-07-22:** with `setKeyguardDisabled(true)` in force, the temporary `svc power stayon usb` was reverted (`stay_on_while_plugged_in = 0`) and the screen driven to `Display State=OFF` / `mWakefulness=Dozing` — ADB stayed connected, `MotionDetectorService` stayed foreground and the camera client stayed held. No keyguard means no USB blocking, so the 8-hour run **can** be monitored over ADB with the screen off, which is also the wanted exhibition state for heat and burn-in. Expect drops again only on a phone that has not yet been through kiosk configuration.
10. **Operator PIN: "not configured" is indistinguishable from "wrong PIN".** (Not the cause on the A07 — see item 12 — but still a real usability defect worth fixing.) `DetectorStore.verifyOperatorPin` returns `PinVerification(false)` when `OPERATOR_PIN_HASH` is absent, so on a fresh install every PIN entry is rejected as `INCORRECT_PIN`. This is the leading explanation for both symptoms reported on 2026-07-22 — the kiosk toggle not sticking and maintenance mode refusing the PIN — because both paths verify the PIN before doing anything else, and `operator_pin_missing` is separately a readiness blocker. Unverified while ADB is down. Also note the lockout: `MAX_PIN_ATTEMPTS = 5`, then `PIN_LOCKOUT_MS = 30_000` (30 s). The panel shows the «Створіть PIN оператора» block when no PIN exists — its presence is the quickest confirmation.
11. **The event log keeps only the last 20 entries.** `MotionDetectorService.MAX_EVENTS = 20`, and `recordEvent` calls `pruneTo(keep)` on every trigger (`DELETE FROM motion_events WHERE id NOT IN (… ORDER BY timestampMs DESC LIMIT :keep)`). So events do prune themselves automatically and nothing needs clearing by hand — but it also means the planned 100+ trigger acceptance run **cannot be evidenced from the in-app log**, which will only ever show the last 20. Capture trigger counts another way (`diagnostics.eventCount` is also capped; `serviceStarts`/`analyzedFrameCount` are not) or raise the cap before the run.
12. **FIXED in 1.3.1 (2026-07-22).** The render condition is now `isDeviceOwner && (!isDefaultHomeApp || !isLockTaskAllowed)`, so the button appears whenever either half of the policy is missing; `applyDeviceOwnerPolicies` is idempotent. `factoryResetProtection` was also added to the diagnostics list in the same build, because «Експорт JSON» opens a share sheet that a phone in Lock Task cannot service. Gate evidence for the build: lint clean, 13/13 unit tests, `npm run build` clean, 11/11 native unit tests, signed `assembleRelease`, certificate SHA-256 matches the permanent key, `Lcom/getcapacitor/annotation/CapacitorPlugin;` present in the packaged DEX, installed with `adb install -r` (Device Owner and Lock Task both survived), operator panel rendered with no crash. Rollback APK of the previous build kept at `/home/alex/exhibit-rollback/app-1.3.0-vc5.apk`. **Beware a false alarm when running the DEX check:** `grep` without `-a` treats `classes.dex` as binary and reports 0 matches even on a healthy build. Original defect description follows. **DEFECT (worked around on the A07 on 2026-07-22, fixed in code as above) — the operator panel cannot configure the kiosk once the app is already the Home app. All three entry points close at once.** The workaround below was executed successfully: HOME was pointed at the Samsung launcher with `adb shell cmd package set-home-activity com.sec.android.app.launcher/.activities.LauncherActivity`, which made `isDefaultHomeApp` false, the «Налаштувати Home і Lock Task» button appeared, the operator pressed it, and `applyDeviceOwnerPolicies` restored HOME to the exhibit itself. **This still needs a code fix before the next device is commissioned** — the next installer will not have ADB or this note. Note the workaround requires ADB, which the same phone drops whenever its screen locks (item 9), so on a phone with a screen lock this defect is close to unrecoverable in the field. `applyDeviceOwnerPolicies` (the only thing that ever calls `setLockTaskPackages`) is reachable from exactly three places, and on the A07 every one is unreachable:
    - **`configureKiosk`** — `NativeDetectorPanel.tsx:404` renders the «Налаштувати Home і Lock Task» button only when `isDeviceOwner && !isDefaultHomeApp`. HOME already resolves to `.MainActivity`, so `isDefaultHomeApp` is true and the button never renders. The condition conflates "is the Home app" with "policies applied", which are independent.
    - **`setAutoStartAfterReboot`** — the «Увімкнути kiosk і автозапуск» button is `disabled={!pinIsValid || !kioskState.autoStartReady}`. `autoStartReady` requires `lockTaskAllowed`, which requires `isLockTaskPermitted`, which requires `setLockTaskPackages` to have already run. Circular: the action that would apply the policies is disabled until the policies are applied.
    - **`lockKiosk`** — the «Повернути kiosk» button does not check `autoStartReady`, so it would work, but it renders only when `maintenanceMode` is true, and the only way in (`unlockKiosk`) renders only when `autoStartAfterRebootEnabled` is true, which it is not.

    Confirmed against the live phone on 2026-07-22: `mLockTaskPackages` empty, `Device Owner Type: 0`, operator PIN **is** configured (the panel shows the «PIN оператора для увімкнення» field, which renders only when `operatorPinConfigured` is true — so the earlier "PIN was never created" theory in item 9 is wrong for this device).

    **Workaround without a rebuild:** make `isDefaultHomeApp` false so the `configureKiosk` button appears — point HOME at the Samsung launcher (`adb shell cmd package set-home-activity com.sec.android.app.launcher/…`), tap the button in the panel, and `applyDeviceOwnerPolicies` restores HOME to the exhibit itself via `addPersistentPreferredActivity`. **Proper fix:** key the button off `!isLockTaskAllowed` rather than `!isDefaultHomeApp`, and reconsider gating the enable button on a readiness flag that the button itself is required to satisfy.

## Remaining production gate

A reproducible signed release build with the existing signing identity is done and tagged, and Device Owner is now provisioned on the target phone. The remaining gates are the operator's `configureKiosk` run (Lock Task packages and persistent Home are still unset), a cold-boot resume test that only becomes meaningful after it, and an 8-hour Galaxy A07 run with charging, heat observation, 100+ triggers, cold boot, route loss, app switching, camera permission loss, storage pressure, and repeated custom narration playback. Automated results must never be presented as physical readiness.
